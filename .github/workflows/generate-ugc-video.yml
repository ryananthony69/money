name: Generate Infinite Money Glitch UGC Video V3

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  generate-video-v3:
    runs-on: ubuntu-24.04
    timeout-minutes: 60

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install media, natural voice, and legacy talking-head client
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg espeak-ng libsndfile1 fonts-dejavu-core fonts-liberation
          python -m pip install --upgrade pip
          pip install kokoro-onnx soundfile gdown==5.2.0 numpy Pillow
          pip install "gradio_client==0.8.1" "websockets==11.0.3"

      - name: Restore and patch pinned V3 renderer
        run: |
          curl -fL --retry 4 --retry-delay 3 \
            -o render_v3.py \
            https://raw.githubusercontent.com/ryananthony69/money/6a7b1e350b77079e5044b0b2932d875b48ab69ba/render_v3.py
          python patch_v3_chunked.py
          python -m py_compile render_v3.py

      - name: Download Kokoro natural voice model
        run: |
          mkdir -p models
          curl -fL --retry 4 --retry-delay 3 \
            -o models/kokoro-v1.0.int8.onnx \
            https://github.com/thewh1teagle/kokoro-onnx/releases/download/model-files-v1.0/kokoro-v1.0.int8.onnx
          curl -fL --retry 4 --retry-delay 3 \
            -o models/voices-v1.0.bin \
            https://github.com/thewh1teagle/kokoro-onnx/releases/download/model-files-v1.0/voices-v1.0.bin
          test -s models/kokoro-v1.0.int8.onnx
          test -s models/voices-v1.0.bin

      - name: Generate chunked model-driven talking creator video
        env:
          KOKORO_VOICE: am_michael
          KOKORO_SPEED: "1.03"
          TALKING_HEAD_SPACES: Beer1819/LipSyncerFull,peterpeter8585/LipSyncer
          TALKING_HEAD_CHUNK_SECONDS: "8.0"
        run: |
          set -o pipefail
          python render_v3.py 2>&1 | tee render-v3.log

      - name: Validate final media and provenance
        run: |
          test -s output/infinite-money-glitch-ugc-v3.mp4
          test -s output/thumbnail-v3.jpg
          test -s output/report-v3.json
          ffprobe -v error -select_streams v:0 \
            -show_entries stream=codec_name,width,height,r_frame_rate \
            -of default=noprint_wrappers=1 output/infinite-money-glitch-ugc-v3.mp4
          ffprobe -v error -select_streams a:0 \
            -show_entries stream=codec_name,sample_rate,channels \
            -of default=noprint_wrappers=1 output/infinite-money-glitch-ugc-v3.mp4
          python - <<'PY'
          import json
          from pathlib import Path
          report = json.loads(Path('output/report-v3.json').read_text())
          assert report['resolution'] == '1080x1920'
          assert report['voice_engine'] == 'Kokoro ONNX'
          assert report['robotic_fallback'] is False
          assert report['still_image_mouth_warp'] is False
          assert 'LipSyncer' in report['talking_head_engine']
          print(report)
          PY

      - name: Upload final V3 video artifact
        uses: actions/upload-artifact@v4
        with:
          name: infinite-money-glitch-ugc-v3
          path: |
            output/infinite-money-glitch-ugc-v3.mp4
            output/thumbnail-v3.jpg
            output/transcript-v3.txt
            output/report-v3.json
          if-no-files-found: error
          compression-level: 0
          retention-days: 30

      - name: Upload diagnostics on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: infinite-money-glitch-v3-diagnostics
          path: |
            render-v3.log
            work-v3/voice.wav
            work-v3/creator-square.jpg
            work-v3/talking-head.mp4
            work-v3/talking-head-chunks/
          if-no-files-found: warn
          retention-days: 7
